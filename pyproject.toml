[project]
name = "recipe-hackathon"
version = "0.1.0"
description = "Parse cookbook epubs into a recipe database"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["."]
addopts = "-x -s"
